# Neon PostgreSQL Database
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/database?sslmode=require

# Expo Push Notifications (Optional - for rate limiting)
# Get from: https://expo.dev/accounts/[account]/settings/access-tokens
EXPO_ACCESS_TOKEN=

# Binance WebSocket Relay Server URL (for US users)
# This should point to your Railway backend
NEXT_PUBLIC_RELAY_URL=https://alertachart-backend-production.up.railway.app

# Development
NODE_ENV=development
